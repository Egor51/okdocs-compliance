<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.okdocs</groupId>
        <artifactId>compliance-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>compliance-app</artifactId>
    <name>compliance-app</name>
    <description>Combined launcher: api + worker в одном процессе для локальной разработки</description>

    <dependencies>
        <!-- api и worker как библиотеки — не как fat jar -->
        <dependency>
            <groupId>io.okdocs</groupId>
            <artifactId>compliance-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.okdocs</groupId>
            <artifactId>compliance-worker</artifactId>
        </dependency>

        <!-- Test: combined-контекст boot-smoke на реальном PostgreSQL + @EmbeddedKafka (§6). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!--
              Combined-контекст IT (*IT) — failsafe в фазе verify, на Testcontainers PostgreSQL +
              @EmbeddedKafka. Требует запущенного Docker. Доказывает, что api- и worker-бины
              сосуществуют в одном Spring-контексте и объединённый application.yml всех удовлетворяет.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
